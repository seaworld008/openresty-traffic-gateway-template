server {
    listen 80;
    server_name admin.example.test;

    access_log /var/log/openresty/admin_example_test.access.log structured_json buffer=256k flush=1s;
    error_log  /var/log/openresty/admin_example_test.error.log warn;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type text/plain;
        try_files $uri =404;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    http2 on;
    server_name admin.example.test;

    access_log /var/log/openresty/admin_example_test.access.log structured_json buffer=256k flush=1s;
    error_log  /var/log/openresty/admin_example_test.error.log warn;

    ssl_certificate     /etc/letsencrypt/live/admin.example.test/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/admin.example.test/privkey.pem;
    include /etc/nginx/snippets/ssl-common.conf;
    include /etc/nginx/snippets/security-headers.conf;

    location / {
        limit_req zone=per_ip_admin burst=10 nodelay;
        include /etc/nginx/snippets/proxy-common.conf;
        proxy_pass http://admin_backend;
    }
}
